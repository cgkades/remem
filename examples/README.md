# Remem Example

Build Remem from the repository root, then use `examples/opencode.json` as a reference for an
OpenCode plugin tuple. The example memory directory contains a Phoenix decision note.

For a real project, place notes under `.remem/memory` or configure an existing Markdown/Obsidian
directory. Restart OpenCode after changing plugin configuration.
