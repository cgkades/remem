# ReMem branding assets

`remem-logo.png` is the primary repository logo used by the root README.

Keep the product wordmark styled as **ReMem** in visual branding. Package names, CLI commands, file paths, and code identifiers remain lowercase `remem` where required.
